// Active brand barrel — scenes import tokens from here, never from a profile
// directly, so swapping profiles needs no scene edits. The golden project uses
// the default profile.
export * from "./profiles/default/style-guide";
